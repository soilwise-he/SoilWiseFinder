<?xml version="1.0" encoding="UTF-8" ?>

<schema name="solr-schema" version="1.7">

    <!-- Primary Key -->
    <field name="identifier" type="string" indexed="true" stored="true" required="true"/>
    <uniqueKey>identifier</uniqueKey>

    <field name="title" type="text_general" indexed="true" stored="true" multiValued="false"/>
    <field name="abstract" type="text_general" indexed="true" stored="true" multiValued="false"/>

    <field name="type" type="string" indexed="true" stored="true" multiValued="false"/>

    <field name="persons" type="text_name" indexed="true" stored="true" multiValued="true"/>
    <field name="organizations" type="text_name" indexed="true" stored="true" multiValued="true"/>
    <field name="view_authors" type="string" indexed="true" stored="true" multiValued="true"/>
    <field name="soilmission" type="boolean" indexed="true" stored="true" multiValued="false"/>
    <field name="license" type="string" indexed="true" stored="true" multiValued="false"/>
    <field name="_version_" type="plong" indexed="false" stored="false"/>

    <field name="contact_persons" type="text_name" indexed="true" stored="true" multiValued="true"/>
    <field name="contact_organizations" type="text_name" indexed="true" stored="true" multiValued="true"/>
    <field name="view_contacts" type="string" indexed="true" stored="true" multiValued="true"/>

    <field name="sources" type="string" indexed="true" stored="true" multiValued="true"/>
    <field name="language" type="string" indexed="true" stored="true" multiValued="false"/>
    <field name="links" type="string" indexed="true" stored="true" multiValued="true"/>
    <field name="projects" type="string" indexed="true" stored="true" multiValued="true"/>
    <field name="project_acronyms" type="string" indexed="true" stored="true" multiValued="true"/>
    <field name="subjects" type="string" indexed="true" stored="true" multiValued="true"/>
    <field name="matched_subjects" type="string" indexed="true" stored="true" multiValued="true"/>
    <field name="thumbnail" type="string" indexed="true" stored="true" multiValued="false"/>

    <!-- Temporal Fields -->
    <field name="date_publication" type="pdate" indexed="true" stored="true" multiValued="false"/>
    <field name="date_creation" type="pdate" indexed="true" stored="true" multiValued="false"/>
    <field name="date_revision" type="pdate" indexed="true" stored="true" multiValued="false"/>
    <field name="date" type="pdate" indexed="true" stored="true" multiValued="false"/>
    <field name="temporal_start" type="pdate" indexed="true" stored="true" multiValued="false"/>
    <field name="temporal_end" type="pdate" indexed="true" stored="true" multiValued="false"/>
    <field name="temporal_range" type="date_range" indexed="true" stored="true" multiValued="true"/>
    <field name="date_harvest" type="pdate" indexed="true" stored="true" multiValued="false"/>


    
    <!-- Spatial and Geometry Fields -->
    <field name="spatial" type="location_rpt" indexed="true" stored="true"/>

    <!-- All free-text searchable fields should be copied in here -->

    <field name="text_all" type="text_general" indexed="true" stored="false" multiValued="true"/>

    <copyField source="title" dest="text_all"/>
    <copyField source="abstract" dest="text_all"/>
    <copyField source="view_authors" dest="text_all"/>
    
    <!-- FieldTypes -->

    <fieldType name="text_name" class="solr.TextField" positionIncrementGap="100">
        <analyzer>
            <tokenizer name="standard"/>
            <filter name="lowercase"/>
        </analyzer>
    </fieldType>

    <!-- The StrField type is not analyzed, but indexed/stored verbatim. -->
    <fieldType name="string" class="solr.StrField" sortMissingLast="true"/>
    <fieldType name="strings" class="solr.StrField" sortMissingLast="true" multiValued="true"/>

    <!-- boolean type: "true" or "false" -->
    <fieldType name="boolean" class="solr.BoolField" sortMissingLast="true"/>
    <fieldType name="booleans" class="solr.BoolField" sortMissingLast="true" multiValued="true"/>

    <!--
      Numeric field types that index values using KD-trees.
      Point fields don't support FieldCache, so they must have docValues enabled if needed for sorting, faceting, functions, etc.
      This is the default, so it does not need to be set explicitly.
    -->
    <fieldType name="pint" class="solr.IntPointField"/>
    <fieldType name="pfloat" class="solr.FloatPointField"/>
    <fieldType name="plong" class="solr.LongPointField"/>
    <fieldType name="pdouble" class="solr.DoublePointField"/>

    <fieldType name="pints" class="solr.IntPointField" multiValued="true"/>
    <fieldType name="pfloats" class="solr.FloatPointField" multiValued="true"/>
    <fieldType name="plongs" class="solr.LongPointField" multiValued="true"/>
    <fieldType name="pdoubles" class="solr.DoublePointField" multiValued="true"/>
    <fieldType name="random" class="solr.RandomSortField" indexed="true"/>

    <!-- The format for this date field is of the form 1995-12-31T23:59:59Z, and
          is a more restricted form of the canonical representation of dateTime
          http://www.w3.org/TR/xmlschema-2/#dateTime
          The trailing "Z" designates UTC time and is mandatory.
          Optional fractional seconds are allowed: 1995-12-31T23:59:59.999Z
          All other components are mandatory.

          Expressions can also be used to denote calculations that should be
          performed relative to "NOW" to determine the value, ie...

                NOW/HOUR
                  ... Round to the start of the current hour
                NOW-1DAY
                  ... Exactly 1 day prior to now
                NOW/DAY+6MONTHS+3DAYS
                  ... 6 months and 3 days in the future from the start of
                      the current day

      -->
    <!-- KD-tree versions of date fields -->
    <fieldType name="pdate" class="solr.DatePointField"/>
    <fieldType name="pdates" class="solr.DatePointField" multiValued="true"/>
    <fieldType name="date_range" class="solr.DateRangeField"/>

    <!-- A general text field that has reasonable, generic
          cross-language defaults: it tokenizes with StandardTokenizer,
          removes stop words from case-insensitive "stopwords.txt"
          (empty by default), and down cases.  At query time only, it
          also applies synonyms.
    -->
    <fieldType name="text_general" class="solr.TextField" positionIncrementGap="100" multiValued="true">
        <analyzer type="index">
            <tokenizer name="standard"/>
            <filter name="stop" ignoreCase="true" words="stopwords.txt"/>
            <!-- in this example, we will only use synonyms at query time
            <filter name="synonymGraph" synonyms="index_synonyms.txt" ignoreCase="true" expand="false"/>
            <filter name="flattenGraph"/>
            -->
            <filter name="lowercase"/>
        </analyzer>
        <analyzer type="query">
            <tokenizer name="standard"/>
            <filter name="stop" ignoreCase="true" words="stopwords.txt"/>
            <filter name="synonymGraph" synonyms="synonyms.txt" ignoreCase="true" expand="true"/>
            <filter name="lowercase"/>
        </analyzer>
    </fieldType>

    <!-- A custom field type for full-text search -->
    <fieldType name="text_tsvector" class="solr.TextField" positionIncrementGap="100">
        <analyzer type="index">
            <tokenizer class="solr.StandardTokenizerFactory"/>
            <filter class="solr.LowerCaseFilterFactory"/>
            <filter class="solr.StopFilterFactory" ignoreCase="true" words="stopwords.txt"/>
            <filter class="solr.PorterStemFilterFactory"/>
        </analyzer>
        <analyzer type="query">
            <tokenizer class="solr.StandardTokenizerFactory"/>
            <filter class="solr.LowerCaseFilterFactory"/>
            <filter class="solr.StopFilterFactory" ignoreCase="true" words="stopwords.txt"/>
            <filter class="solr.PorterStemFilterFactory"/>
        </analyzer>
    </fieldType>

    <!-- A geospatial field type that supports multiValued and polygon shapes.
      For more information about this and other spatial fields see:
      https://solr.apache.org/guide/solr/latest/query-guide/spatial-search.html
    -->
    <!-- <fieldType name="location_rpt" class="solr.SpatialRecursivePrefixTreeFieldType"
                geo="true" distErrPct="0.025" maxDistErr="0.001" distanceUnits="kilometers" /> -->
    <fieldType name="location_rpt" class="solr.SpatialRecursivePrefixTreeFieldType"
               spatialContextFactory="JTS"
               autoIndex="true"
               geo="true"
               validationRule="repairBuffer0"
               distErrPct="0.025"
               maxDistErr="0.001"
               distanceUnits="kilometers"/>

    <fieldType name="geom" class="solr.SpatialRecursivePrefixTreeFieldType"
               spatialContextFactory="Geo3D"
               prefixTree="s2"
               planetModel="WGS84"/>

    <fieldType name="text_nl" class="solr.TextField" positionIncrementGap="100">
        <analyzer>
            <tokenizer name="standard"/>
            <filter name="lowercase"/>
            <filter name="stop" ignoreCase="true" words="lang/stopwords_nl.txt" format="snowball"/>
            <filter name="stemmerOverride" dictionary="lang/stemdict_nl.txt" ignoreCase="false"/>
            <filter name="snowballPorter" language="Dutch"/>
        </analyzer>
    </fieldType>

    <!-- Similarity is the scoring routine for each document vs. a query.
        A custom Similarity or SimilarityFactory may be specified here, but
        the default is fine for most applications.
        For more info: https://solr.apache.org/guide/solr/latest/indexing-guide/schema-elements.html#similarity
    -->
    <!--
      <similarity class="com.example.solr.CustomSimilarityFactory">
        <str name="paramkey">param value</str>
      </similarity>
    -->


</schema>
